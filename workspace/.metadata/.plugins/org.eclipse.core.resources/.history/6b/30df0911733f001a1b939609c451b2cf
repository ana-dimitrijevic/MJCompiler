program MJProgram
	int a, b;
	char arr[];
{

	void fun () char c; {
		//if (len(arr) != 0) {
		//	c = arr[0];
		//	print(c,5);
		//}
		
		print('f');
		print('u');
		print('n');
		return;
	}
	
	bool fun2 (int a, int b) int sum; {
		if ((a + b) > 10) {
			sum = a+b;
			print(sum);
			return true;
		}
		else {
			return false;
		}
	}
	
	int inc(int incrementable) {
		incrementable++;
		return incrementable;
	}
	
	void recursive(int a, int b) {	
		 recursive(a, b);		
	}

	int recursive2(int a, int b) {	
		 return recursive(a, b);		
	}
	
	void main() int i; {
	
		arr = new char[4];
		arr[0] = 'a';
		if(fun2(6,6)) {

			i=0;			
			for(;i<3;i++){
				fun();
			}
	
		}
		
		funn();
		i();
		fun(3);
		fun2(1,'a');
	}
}